services:
  - type: web
    name: polybot
    env: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    runtime: python3.11.9
    envVars:
      - key: BOT_TOKEN
        sync: false
